'use client';

// Placeholder component retained for potential future map implementations.
// Removed Google Maps API usage to avoid relying on external Google services.
export default function GoogleMap() {
  return null;
}
